import React, { Fragment } from 'react'
import Signout from '../../forms/signout'

const Home = () => (
    <Fragment>
        <h1>User home</h1>
        <Signout />
    </Fragment>
)

export default Home